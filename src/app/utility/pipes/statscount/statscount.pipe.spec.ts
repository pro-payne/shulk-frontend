import { StatscountPipe } from './statscount.pipe';

describe('StatscountPipe', () => {
  it('create an instance', () => {
    const pipe = new StatscountPipe();
    expect(pipe).toBeTruthy();
  });
});
